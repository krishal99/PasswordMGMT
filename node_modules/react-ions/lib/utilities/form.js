'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formSchemaToKeyVal = undefined;

var _immutable = require('immutable');

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

// Receives an immutable map of a schema, then assigns the nested values to the keys.
var formSchemaToKeyVal = exports.formSchemaToKeyVal = function formSchemaToKeyVal(schema) {
  return (0, _immutable.fromJS)(schema).flatMap(function (value, key) {
    return (0, _immutable.Map)(_defineProperty({}, key, value.get('value')));
  });
};